import { useState } from "react";

const [ count, setCount ] = useState(0); // 0 is the default value
const [ name, setName ] = useState(''); // empty string is the default value
const [ name, setName ] = useState('Raghul'); // raghul is the default value
